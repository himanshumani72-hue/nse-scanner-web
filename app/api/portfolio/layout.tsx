// Layout for portfolio API routes — required for
// subdirectory route resolution (alerts/, sectors/)
export default function PortfolioApiLayout({ children }: { children: React.ReactNode }) {
  return children;
}
